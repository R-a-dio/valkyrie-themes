{{define "content"}}
<div class="container">
    <div class="section">
        <div class="fixed-grid has-4-cols">
            <div class="grid">
                <div class="cell is-col-span-1 box has-background-secondary-1 mb-0">
                    <div class="grid is-fullheight">
                        <div class="cell title is-flex is-justify-content-center is-align-self-center has-text-weight-light mb-0">Streambooth</div>
                    </div>
                </div>
                <div class="cell is-col-span-2 box has-background-secondary-1 mb-0">
                    <div class="grid is-fullheight">
                        <p class="cell is-flex is-justify-content-center is-align-self-center">Radical new stream page where you can do something</p>
                    </div>
                </div>
                <div class="cell is-col-span-1">
                    <div class="grid is-fullheight">
                        <p class="cell title is-flex is-justify-content-center is-align-self-center has-text-weight-bold">{{.User.DJ.Name}}</p>
                    </div>
                </div>
                <div class="cell is-col-span-1">
                    <div class="grid is-fullheight">
                        {{with .ProxyStatus}}
                            {{if $.IsLive}}[LIVE]{{else}}[CONNECTED]{{end}} <p class="cell title">{{.MountName}}</p>
                        {{else}}
                            [DISCONNECTED]
                        {{end}}
                    </div>
                </div>
                <div class="cell is-col-span-1">
                    {{with .ProxyStatus}}
                        Connected for {{.Start | Since}}
                    {{else}}
                        NOT CONNECTED
                    {{end}}
                </div>
                <div class="cell is-col-span-1">
                    {{with .ProxyStatus}}
                        {{.Metadata}}
                    {{else}}
                        NOT CONNECTED
                    {{end}}
                </div>
                <div class="cell is-col-span-1">
                    USERNAME: {{.User.Username}}
                    PASSWORD: SUPER_SECRET
                </div>
                <div class="cell is-col-span-3 is-row-span-2 box has-background-secondary-1 mb-0">
                    <div class="grid"><div class="cell is-col-span-3 is-size-3 has-text-weight-light">Description</div>
                    <p class="cell is-col-span-3">Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text</p>
                    <p class="cell is-col-span-3">Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text</p>
                    <p class="cell is-size-5 has-text-weight-light is-col-span-3">Subdescription</p>
                    <p class="cell is-col-span-3">Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text Text</p>
                </div>
                </div>
                <div class="cell is-col-span-1 box has-background-secondary-1 mb-0">
                    <div class="grid">
                        <div class="cell title has-text-weight-light is-flex is-justify-content-center mb-0">Kill Hanyuu</div>
                        <p class="cell">This kills the hanyuu. You must connect within x seconds after the current song ends.</p>
                        {{$progress := .Status.SongInfo.Start | Since}}
                        <progress id="current-song-progress" class="progress is-large mb-0" value="{{$progress | ToSecond}}" max="{{.Status.Song.Length.Seconds}}"></progress>
                        <button class="cell button booth-button {{if not .ProxyStatus}}disabled{{end}}">Kill Hanyuu</button>
                    </div>
                </div>
                <div class="cell is-col-span-1 box has-background-secondary-1 mb-0">
                    <div class="grid">
                        <div class="cell title has-text-weight-light is-flex is-justify-content-center mb-0">Thread</div>
                        <p class="cell">This is the current thread, you can update it here and use an image by prepending 'image:' to an URL</p>
                        <form>
                            <input id="booth-something-input" class="input has-text-centered" type="text" placeholder="None">
                        </form>
                        <button class="cell button booth-button">Update</button>
                    </div>
                </div>
            </div>
        </div>
        <div class="fixed-grid has-6-cols">
            <div class="grid box has-background-secondary-1">
                <button class="cell button">Button1</button>
                <button class="cell button">Button2</button>
                <button class="cell button">Button3</button>
                <button class="cell button">Button4</button>
                <button class="cell button">Button5</button>
                <button class="cell button">Button6</button>
            </div>
        </div>
    </div>
    {{printjson .}}
</div>
{{end}}