{{define "content"}}
<div class="container">
    <div class="section">
        <div class="grid">
            <div class="cell is-3 notification">
                <p class="title">Daypass</p>
                <p class="tag is-info">{{.Daypass}}</p>
                <p class="is-size-7">Expires at midnight</p>
            </div>
            <div class="cell is-3 notification">
                Welcome {{.User.Username}}.
            </div>
            <div class="cell is-3 notification">
                <button class="button" hx-trigger="click" hx-post="/admin/api/streamer/stop" hx-confirm="Are you sure?">Kill Hanyuu-sama</a>
            </div>
        </div>
    </div>
</div>
{{printjson .}}
{{end}}